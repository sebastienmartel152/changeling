const LastEntry = () => {
    return (<div>
    <h1>Apologies. The story has reached a point where events cannot be resolved. Consult with key actors to resolve issues, and progress anew.</h1>
    
    </div>
    );
  };
  
  export default LastEntry;